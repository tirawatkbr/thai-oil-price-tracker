# Thai Oil Price Scraper — Python dependencies
# Install: pip install -r requirements.txt

# HTTP
requests==2.31.0

# HTML parsing
beautifulsoup4==4.12.3
lxml==5.1.0

# Google Sheets (optional — only needed if GOOGLE_CREDENTIALS_JSON is set)
gspread==6.0.2
google-auth==2.27.0

# Headless browser for Shell (Shadow DOM)
playwright>=1.40.0
